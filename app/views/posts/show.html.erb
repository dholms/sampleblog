<h2><%= @post.title %></h2>

<p>	<%= @post.text %></p>


<h3>Comments</h3>
<%= render @post.comments %>

<h3>Add a comment:</h3>
<%= render "comments/form" %>

<%= link_to 'Back to Posts', posts_path, type: "button", class: "btn btn-primary" %>
<%= link_to 'Edit Post', edit_post_path, type: "button", class: "btn btn-warning" %>